spring.application.name=demo

# Don't change the port
server.port=9001

# for https
server.forward-headers-strategy=framework 

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/apartment_facility?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# 🔑 JWT Security configuration
security.jwt.secret=MySuperSecretKey_ChangeMe   # choose a strong random string
security.jwt.expiration-ms=86400000             # 1 day in milliseconds
